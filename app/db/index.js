// Easy way to switch between mock and impl

import {getTeams, getTeam, getMatches, getGroups, getMatchesByTeamId} from './db-impl';

export {
    getTeams,
    getTeam,
    getMatchesByTeamId,
    getMatches,
    getGroups
}
